// next.config.js
module.exports = {
    reactStrictMode: true, // Ativa o modo estrito do React
    webpack: (config) => {
      // Customizações do Webpack podem ser feitas aqui
      return config;
    },
  };
  